// Empty by default so the Cards page shows its empty state until real
// cards exist. Each card object shape, once populated:
// { id, label, last4, expiry, currency, balance, status: "active" | "pending" }
export const cards = [];